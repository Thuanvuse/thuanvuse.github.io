import sys
from PyQt6.QtWidgets import QApplication, QWidget, QLabel, QPushButton, QVBoxLayout
from PyQt6.QtCore import Qt
from PyQt6.QtGui import QFont

class MaintenanceWindow(QWidget):
    def __init__(self):
        super().__init__()
        self.setWindowTitle("⚠️ BẢO TRÌ HỆ THỐNG ⚠️")
        self.resize(500, 400)
        self.setWindowFlags(Qt.WindowType.WindowStaysOnTopHint)
        self.setup_ui()

    def setup_ui(self):
        layout = QVBoxLayout()
        
        # Thiết lập màu nền và font
        self.setStyleSheet("""
            QWidget {
                background-color: #1a1a1a;
                color: #ffffff;
            }
            QLabel {
                color: #ffffff;
            }
        """)
        
        # Logo hoặc icon bảo trì
        maintenance_icon = QLabel("🔧")
        maintenance_icon.setAlignment(Qt.AlignmentFlag.AlignCenter)
        maintenance_icon.setStyleSheet("""
            font-size: 100px;
            margin: 20px;
            color: #ff6b6b;
        """)
        layout.addWidget(maintenance_icon)
        
        # Tiêu đề
        title = QLabel("⚠️ HỆ THỐNG ĐANG BẢO TRÌ ⚠️")
        title.setAlignment(Qt.AlignmentFlag.AlignCenter)
        title.setStyleSheet("""
            font-size: 28px;
            font-weight: bold;
            color: #ff6b6b;
            margin: 10px;
            padding: 10px;
            background-color: #2d2d2d;
            border-radius: 10px;
        """)
        layout.addWidget(title)
        
        # Thông báo
        message = QLabel(
            "Chúng tôi đang nâng cấp hệ thống để mang đến trải nghiệm tốt hơn cho bạn.\n"
            "Xin lỗi vì sự bất tiện này. Vui lòng quay lại sau!"
        )
        message.setAlignment(Qt.AlignmentFlag.AlignCenter)
        message.setStyleSheet("""
            font-size: 16px;
            margin: 20px;
            padding: 15px;
            background-color: #2d2d2d;
            border-radius: 10px;
        """)
        message.setWordWrap(True)
        layout.addWidget(message)
        
        # Nút đóng
        close_button = QPushButton("ĐÓNG")
        close_button.setStyleSheet("""
            QPushButton {
                background-color: #ff6b6b;
                color: white;
                border: none;
                padding: 15px 30px;
                border-radius: 10px;
                font-size: 18px;
                font-weight: bold;
                margin: 20px;
            }
            QPushButton:hover {
                background-color: #ff5252;
            }
        """)
        close_button.clicked.connect(self.close)
        layout.addWidget(close_button)
        
        self.setLayout(layout)

if __name__ == "__main__":
    app = QApplication(sys.argv)
    window = MaintenanceWindow()
    window.show()
    sys.exit(app.exec())
